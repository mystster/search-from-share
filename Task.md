# プロジェクト背景と残タスク

## プロジェクト背景
本プロジェクト「Search from Share」は、Androidの共有メニューからテキストを受け取り、即座にGoogle検索を行うためのユーティリティアプリです。

当初はFlutterで開発されていましたが、以下の理由により **純粋なAndroidネイティブ (Kotlin)** プロジェクトへ移行しました。

1.  **パフォーマンス**: Flutterエンジンの起動オーバーヘッドを排除し、共有メニュー選択からブラウザ起動までの時間を短縮するため。
2.  **UIレス体験**: 画面遷移時のチラつき（Flutterの描画待ち）をなくし、完全に透明なActivityで処理を行うため。

### 現在の機能
- **検索 (Custom Tab)**: Chrome Custom Tabsを使用してアプリ内で高速に検索を行います。
- **検索**: 外部ブラウザ（デフォルトブラウザ）を起動して検索を行います。
- **ランチャー非表示**: アプリドロワーにはアイコンを表示せず、共有メニューからのみ利用可能です。
- **テキスト処理**: 共有されたテキストからURLや引用符を自動的に除去して検索クエリを生成します。

## 注意事項
- TaskやImplementation Plan、Walkthroughを含め、やりとりはすべて日本語にしてください。
- ただしコミットメッセージは英語にしてください。
- コミットメッセージはConventional Commitsの規則に従ってください。

---

## 残タスク

現在、以下のタスクが未実装として残っています。

- [x] **検索スキップ時のToast通知**
    - 共有されたテキストが空であったり、有効な検索クエリが生成できなかった場合に、ユーザーにその旨を通知するToastを表示する機能。
    - 現在は何もせず終了しているため、フィードバックを追加する。

- [ ] **リファクタリング**
    - `CustomTabActivity` と `ShareActivity` で重複している共有テキスト処理（URL除去、引用符除去、クエリ生成）を共通クラス（例: `SharedTextHandler`）に抽出する。
    - ハードコードされている文字列（Google検索URL、正規表現など）を定数化する。
    - 他にリファクタリング可能な箇所がないか調査する。

- [x] **リリースビルドの失敗の修正**
    - `./gradlew assembleRelease bundleRelease` が失敗する原因を特定し修正する。
    - 現状、署名設定（Signing Config）が `build.gradle.kts` に存在しないため、署名エラーまたはLintエラーの可能性がある。
    - GitHub Actions (`release.yml`) が正常に完了するようにする。

- [x] **バージョニングの自動化**
    - GitHub ActionsのReleaseを実行し、リリースビルドを作成した際、APKに埋め込まれているバージョンがGitタグ（例: `v0.0.4`）と一致していない（`build.gradle.kts` の固定値が使われる）。
    - リリースフローにおいて、Gitタグに基づいて `versionName` と `versionCode` を適切に設定するように修正する。

- [x] **APKファイルのアップロード修正**
    - GitHub ActionsのReleaseフロー完了後、ReleaseページにAPKファイルが添付されていない（AABのみになっている）。
    - 署名設定を追加したことにより、生成されるAPKのファイル名が変更された（`app-release-unsigned.apk` → `app-release.apk`）可能性があるため、`release.yml` を修正してAPKもアップロードされるようにする。

- [x] **アダプティブアイコン対応**
    - 現在のアイコンをAndroid 8.0以降の標準であるアダプティブアイコンに対応させる。
- [x] **アダプティブアイコン対応**
    - 現在のアイコンをAndroid 8.0以降の標準であるアダプティブアイコンに対応させる。
    - アイコンの背景レイヤーの色は「#70D3A8」とする。
    - [ ] アイコンサイズの調整（丸型の場合に少しはみ出すため、縮小調整が必要）

- [ ] **デバッグビルドの識別化**
    - 開発版（Debugビルド）と製品版（Releaseビルド）を一目で区別できるようにする。
    - **バージョン情報**: デバッグビルドの `versionName` には `-dev` などのサフィックスを自動付与する。
    - **アイコン**: デバッグビルドのアイコンには、右上に「dev」などのラベルを追加した専用アイコンを使用する。
