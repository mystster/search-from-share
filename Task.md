# プロジェクト概要とタスク

## プロジェクト背景
本プロジェクト「Search from Share」は、Androidの共有メニューからテキストを受け取り、即座にGoogle検索を行うためのユーティリティアプリです。

当初はFlutterで開発されていましたが、以下の理由により **純粋なAndroidネイティブ (Kotlin)** プロジェクトへ移行しました。

1.  **パフォーマンス**: Flutterエンジンの起動オーバーヘッドを排除し、共有メニュー選択からブラウザ起動までの時間を短縮するため。
2.  **UIレス体験**: 画面遷移時のチラつき（Flutterの描画待ち）をなくし、完全に透明なActivityで処理を行うため。

## 現在の機能 (v1.0.0)
- **検索 (Custom Tab)**: Chrome Custom Tabsを使用してアプリ内で高速に検索を行います。
- **検索**: 外部ブラウザ（デフォルトブラウザ）を起動して検索を行います。
- **ランチャー非表示**: アプリドロワーにはアイコンを表示せず、共有メニューからのみ利用可能です。
- **テキスト処理**: 共有されたテキストからURLや引用符を自動的に除去して検索クエリを生成します。
- **フィードバック**: 検索スキップ時（空文字など）にToastで通知します。
- **開発者向け機能**:
    - **デバッグ識別**: デバッグビルドはアイコンとバージョン名（`-dev`）で区別可能。
    - **CI/CD**: GitHub Actionsによる自動ビルド、テスト、リリース（APK生成・アップロード）。

## 技術スタック
- **言語**: Kotlin (Java 17)
- **アーキテクチャ**: Native Android (Activity-based)
- **ブラウザ連携**: Chrome Custom Tabs (`androidx.browser:browser`)
- **ビルドシステム**: Gradle (Kotlin DSL)
- **CI/CD**: GitHub Actions

---

## 注意事項
- TaskやImplementation Plan、Walkthroughを含め、やりとりはすべて日本語にしてください。
- ただしコミットメッセージは英語にしてください。
- コミットメッセージはConventional Commitsの規則に従ってください。
- 簡単な修正以外は作業前に新しいブランチを作成すること。
- 適切な粒度で適宜コミットを行うこと。

---

## 残タスク

現在、v1.0.0リリースに向けた主要タスクは完了しています。
今後の改善案やバグ修正が発生した場合はここに追記してください。

- [ ] **i18n対応**
    - 現状、`values/strings.xml` に日本語が含まれているため、英語環境でも日本語が表示されてしまう。
    - デフォルトリソース (`values/strings.xml`) を英語化する。
    - 日本語リソース (`values-ja/strings.xml`) を作成し、現在の日本語テキストを移動する。


---

## 完了タスク履歴 (v1.0.0まで)

### 機能実装・改善
- [x] **リファクタリング**: 共有テキスト処理ロジックの共通化（拡張関数）、ハードコード解消、リソース整理、Java 17対応。
- [x] **検索スキップ時のToast通知**: 空テキスト共有時のフィードバック追加。
- [x] **アダプティブアイコン対応**: Android 8.0以降の標準アイコン対応（背景色 #70D3A8）。
- [x] **デバッグビルドの識別化**: バージョン名サフィックス（`-dev`）と専用アイコンの導入。

### CI/CD・ビルド環境
- [x] **リリースビルドの修正**: 署名設定の追加とリリースフローの整備。
- [x] **バージョニングの自動化**: Gitタグに基づく `versionName` / `versionCode` の自動設定。
- [x] **APKアップロード修正**: GitHub ReleasesへのAPK自動添付。
- [x] **CIビルド修正**: `fetch-depth: 0` 設定によるタグ取得エラーの解消。
